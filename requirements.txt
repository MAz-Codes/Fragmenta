--extra-index-url https://download.pytorch.org/whl/cu128

Flask>=2.3.0,<4.0
Flask-CORS>=4.0.0,<7.0
requests>=2.28.0,<3.0

torch==2.7.1
torchaudio==2.7.1

transformers>=5.8.0
huggingface-hub>=1.7.1
safetensors>=0.7.0
accelerate>=1.13.0

pytorch_lightning==2.5.5
einops>=0.8.2
einops-exts>=0.0.4
dill>=0.4.1

librosa>=0.11.0,<0.12
soundfile>=0.13.1
scipy>=1.13,<2
numba>=0.60,<1
numpy>=2.2.6,<3
pandas>=2.2,<3

setuptools<70
tqdm>=4.67,<5.0
psutil>=5.9.0,<8.0
omegaconf>=2.3.0,<3.0
click>=8.1.0,<9.0
Pillow>=9.0.0,<13.0
python-dotenv>=1.0.0,<2.0

pywebview>=4.4.1,<7.0
Pycairo ; sys_platform == 'linux'
PyGObject<3.49 ; sys_platform == 'linux'

# flash-attn — required ONLY for sa3-medium. Small models fall back to
# standard attention. Not installed via pip because the right wheel
# depends on torch version × ABI × Python × CUDA, and pip's resolver
# picks the wrong ABI as often as the right one. Install manually after
# pip install -r requirements.txt by picking the wheel matching your
# `python -c "import torch; print(torch.__version__, torch._C._GLIBCXX_USE_CXX11_ABI)"`
# from https://github.com/Dao-AILab/flash-attention/releases
# Example (Linux, torch 2.7.1+cu128, Python 3.11, cxx11_abi=True):
#   pip install --no-deps https://github.com/Dao-AILab/flash-attention/releases/download/v2.8.3/flash_attn-2.8.3+cu12torch2.7cxx11abiTRUE-cp311-cp311-linux_x86_64.whl
